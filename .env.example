# Copy to .env and fill in your keys. Values are loaded at server startup and
# prefilled in the WebUI (Configuration > ASR > OpenAI Realtime API > WebSocket > API Key,
# and Configuration > LLM > API Key).

# OpenAI API key (for Realtime API WebSocket ASR and for OpenAI-compatible LLM)
OPENAI_API_KEY=
